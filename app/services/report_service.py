# Service rapports — Phase 2 (non implémenté en Phase 1)
# Ce module sera activé en Phase 2 avec les dépendances : openpyxl, python-pptx, weasyprint


def generate_weekly_report(*args, **kwargs):
    """Génère le rapport hebdomadaire — disponible en Phase 2."""
    raise NotImplementedError("Rapports disponibles en Phase 2")


def generate_monthly_report(*args, **kwargs):
    """Génère le rapport mensuel — disponible en Phase 2."""
    raise NotImplementedError("Rapports disponibles en Phase 2")


def generate_project_report(*args, **kwargs):
    """Génère le rapport de projet — disponible en Phase 2."""
    raise NotImplementedError("Rapports disponibles en Phase 2")


def generate_team_kpi_report(*args, **kwargs):
    """Génère le rapport KPI équipe — disponible en Phase 2."""
    raise NotImplementedError("Rapports disponibles en Phase 2")


def export_to_excel(*args, **kwargs):
    """Exporte les données vers Excel — disponible en Phase 2."""
    raise NotImplementedError("Export Excel disponible en Phase 2")


def export_to_pdf(*args, **kwargs):
    """Exporte les données vers PDF — disponible en Phase 2."""
    raise NotImplementedError("Export PDF disponible en Phase 2")


def export_to_pptx(*args, **kwargs):
    """Exporte les données vers PowerPoint — disponible en Phase 2."""
    raise NotImplementedError("Export PowerPoint disponible en Phase 2")
