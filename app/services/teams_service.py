# Service Teams — Phase 3 (non implémenté en Phase 1)
# Ce module sera activé en Phase 3 avec les dépendances : aiohttp, apscheduler


async def send_alert_notification(*args, **kwargs):
    """Envoie une notification d'alerte via Teams — disponible en Phase 3."""
    raise NotImplementedError("Teams integration disponible en Phase 3")


async def send_flag_notification(*args, **kwargs):
    """Envoie une notification de signalement via Teams — disponible en Phase 3."""
    raise NotImplementedError("Teams integration disponible en Phase 3")


async def send_done_notification(*args, **kwargs):
    """Envoie une notification de fin de projet via Teams — disponible en Phase 3."""
    raise NotImplementedError("Teams integration disponible en Phase 3")


async def send_weekly_recap(*args, **kwargs):
    """Envoie le récapitulatif hebdomadaire via Teams — disponible en Phase 3."""
    raise NotImplementedError("Teams integration disponible en Phase 3")


async def test_webhook(*args, **kwargs):
    """Teste la connexion au webhook Teams — disponible en Phase 3."""
    raise NotImplementedError("Teams integration disponible en Phase 3")
